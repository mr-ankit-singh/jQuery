/*
#__ jQuery Html __#

 * jQuery DOM Manipulation

    -One very important part of jQuery is the possibility to manipulate the DOM.
    -jQuery comes with a bunch of DOM related methods that make it easy to access and manipulate elements and attributes.

    DOM

    -Programmatic representation of the webpage
    -Manipulated by various language like Javascript jQuery and angular


 * Methods

 1) Get Content - text(), html(), val() and attr()

    Three simple, but useful, jQuery methods for DOM manipulation are:

    -text() - Sets or returns the text content of selected elements
    -html() - Sets or returns the content of selected elements (including HTML markup)
    -val() - Sets or returns the value of form fields
    -attr() -  method is used to get attribute values.


 2) Set Content - text(), html(), and val()

    We will use the same three methods from the previous page to set content:

    -text() - Sets or returns the text content of selected elements
    -html() - Sets or returns the content of selected elements (including HTML markup)
    -val() - Sets or returns the value of form fields
    -attr() - it is used to change the attribute of element

*/